{
  "name": "mono-actions-test",
  "private": true,
  "workspaces": {
    "packages": [
      "app-1",
      "app-2"
    ]
  },
  "scripts": {
    "add-lock-file": "cp yarn.lock app-1 && cp yarn.lock app-2"
  }
}
